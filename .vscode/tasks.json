{
    "version": "2.0.0",
    "tasks": [
      {
        "type": "shell",
        "label": "build",
        "command": "meson compile",
        "options": { "cwd": "${workspaceFolder}/build/" },
        "group": {
          "kind": "build",
          "isDefault": true
        },
      },
      {
        "type": "shell",
        "label": "run",
        "command": "./engine.exe",
        "options": { "cwd": "${workspaceFolder}/build/" },
      }
    ]
  }